{
  "name": "app-bot",
  "version": "0.1.0",
  "repository": "https://github.com/xMarok/app-bot",
  "author": "Marok",
  "license": "UNLICENSED",
  "dependencies": {
    "discord.js": "^12.3.1",
    "dotenv": "^8.2.0",
    "node-persist": "^3.1.0"
  },
  "scripts": {
    "start": "pm2 start ecosystem.config.js --only app-bot",
    "test-start": "pm2 start ecosystem.config.js --only test-app-bot",
    "restart": "yarn compile && pm2 restart app-bot",
    "test-restart": "yarn compile && pm2 restart test-app-bot",
    "compile": "tsc"
  },
  "devDependencies": {
    "@types/node": "^14.10.3",
    "@types/node-persist": "^3.1.0",
    "@types/ws": "^7.2.6",
    "@yarnpkg/pnpify": "^2.2.1",
    "typescript": "^4.0.2"
  }
}
