{
  "name": "app-bot",
  "version": "0.1.0",
  "description": "Discord bot for the Warrior discord server, Skyhold.",
  "main": "eyir.js",
  "repository": "https://github.com/xMarok/eyir",
  "author": "Marok",
  "license": "UNLICENSED",
  "type": "module",
  "dependencies": {
    "discord.js": "^12.3.1",
    "dotenv": "^8.2.0",
    "node-persist": "^3.1.0"
  },
  "scripts": {
    "start": "pm2 start ecosystem.config.cjs --only app-bot",
    "test-start": "pm2 start ecosystem.config.cjs --only test-app-bot",
    "restart": "yarn compile && pm2 restart all",
    "compile": "tsc",
    "format": "prettier --write src"
  },
  "devDependencies": {
    "@types/node": "^14.10.3",
    "@types/node-persist": "^3.1.0",
    "@types/ws": "^7.2.6",
    "@yarnpkg/pnpify": "^2.2.1",
    "prettier": "^2.1.2",
    "typescript": "^4.0.2"
  }
}
